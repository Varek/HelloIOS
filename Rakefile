# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'

begin
  require 'bundler'
  Bundler.require
rescue LoadError
end

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'HelloIOS'
  # app.identifier = 'com.yourdomain.exampleapp'
  # app.codesign_certificate = 'iPhone Developer: Andre Rieck (FN5HR6HW99)'
  # app.provisioning_profile = '~/Coding/rubymotion.mobileprovision'
end
